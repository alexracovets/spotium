import { LayoutSpotium } from '@templates'

import { ChildrenType } from '@types'

export default function RootLayout({ children }: ChildrenType) {
  return <LayoutSpotium>{children}</LayoutSpotium>
}
